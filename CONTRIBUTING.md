# Contributing to the portal

This repository accepts changes to the discovery website only. Keep catalog
metadata, thumbnails, and mod submissions in
[`ofs-mod-catalog`](https://github.com/OFS-Modding/ofs-mod-catalog).

Before opening a pull request:

- run `npm test`;
- verify keyboard, mobile, and loading/error behavior;
- do not add credentials or catalog signing material.
